<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type'/>
  </head>
  <body>
    <h1>Order Summary
       </h1>
      <p>You have ordered the following item(s),
    Your order id is   <%= @order.id %> '</br>' And
    Your Total is $ <%= @order.total_cents / 100 %>
    your ordered items:</p>
    <table>
      <tr>
        <th>
          Product Name
        </th>
        <th>
          Quantity
        </th>
        <th>
          Unit Price
        </th>
        <th>
          Total Price
        </th>
      </tr>
      <% @order.line_items.each do |item| %>
      <tr>
        <td>
          <%= item.product.name %>
        </td>
        <td>
          <%= item.quantity %>
        </td>
        <td>
          <%= item.item_price_cents %>
        </td>
        <td>
          <%= item.total_price_cents %>
        </td>
      </tr>
      <% end %>
    </table>


    <p>Thanks for shopping with us.  Have a great day!</p>
  </body>
</html>
